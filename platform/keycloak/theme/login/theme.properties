# MedAgent branded login theme (02 §1). Extends the classic keycloak login theme
# (stable, simple HTML structure) and replaces its stylesheet with our own brand CSS
# — teal accent, clean centered card, MedAgent lockup. Kept offline-safe (no external
# fonts/CSS) for air-gapped/pilot deploys.
parent=keycloak
styles=css/login.css
