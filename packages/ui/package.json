{
  "name": "@medagent/ui",
  "version": "0.1.0",
  "private": true,
  "description": "MedAgent design system — semantic tokens + presentational components (docs/solution/06 §4). Consumed as TypeScript source via Next.js transpilePackages.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./tailwind-preset": "./tailwind-preset.ts",
    "./styles/tokens.css": "./styles/tokens.css"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(ui) lint runs from apps/web flat config in S1; package-local eslint lands S2\""
  },
  "peerDependencies": {
    "react": "^19.2.0"
  },
  "dependencies": {
    "lucide-react": "^0.525.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "typescript": "^5.8.0"
  }
}
