"""Shared building blocks for the MedAgent Python services (Stage C).

One source of truth for cross-service concerns that were copy-pasted into
core-api, agent-service and notify-service: structured JSON logging, Prometheus
telemetry, and Keycloak JWT/JWKS authentication.
"""
