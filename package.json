{
  "name": "medagent-platform",
  "private": true,
  "packageManager": "pnpm@10.6.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @medagent/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "pnpm": {
    "overrides": {
      "sharp@<0.35.0": ">=0.35.0",
      "postcss@<8.5.10": ">=8.5.10"
    }
  }
}
