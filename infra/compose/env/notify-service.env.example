# notify-service env — template only, dummy values (10 §5).
# Real environments: rendered from SOPS (infra/secrets/{env}.enc.yaml) at deploy.
KEYCLOAK_CLIENT_ID=notify-service
KEYCLOAK_CLIENT_SECRET=changeme-dummy
# SMS gateway adapter (pilot); mailpit covers email locally
SMS_GATEWAY_API_KEY=changeme-dummy
