# core-api service env — template only, dummy values (10 §5).
# Real environments: rendered from SOPS (infra/secrets/{env}.enc.yaml) at deploy.
# Locally optional: docker-compose.yml already sets working dev defaults.
KEYCLOAK_CLIENT_ID=core-api
KEYCLOAK_CLIENT_SECRET=changeme-dummy
FACE_WEBHOOK_HMAC_KEY_ID=dev-key-1
FACE_WEBHOOK_HMAC_SECRET=changeme-dummy
# secondary key during 90-day dual-key rotation (05 §3, 10 §5)
FACE_WEBHOOK_HMAC_KEY_ID_NEXT=
FACE_WEBHOOK_HMAC_SECRET_NEXT=
S3_ACCESS_KEY=changeme-dummy
S3_SECRET_KEY=changeme-dummy
